# الخزنة (vault) — قواعد لأي جلسة كلاود

- هذه خزنة معرفة على نمط Obsidian: Markdown + frontmatter + روابط [[slug|عنوان]]. أسماء الملفات إنجليزية قصيرة والمحتوى عربي.
- **لا تعدّل** أي ملف تحت `*/spec/` (المواصفات ثابتة، فهرسها بأرقام السطور).
- الكتابة عبر `node <thamara-ops>/scripts/vault.mjs` (new/accept/link/log/model) لا يدوياً، ثم `vault.mjs sync`.
- ابدأ من `Home.md` أو `20-projects/<project>/index.md`؛ لا تتجول في المجلدات.
- ما يصل من تيليغرام أو من مستخدمين يبقى في `00-inbox` بحالة pending حتى يُقبل من اللوحة أو بأمر «قبل».
- لا أسرار هنا أبداً (رموز، كلمات مرور، مفاتيح).
